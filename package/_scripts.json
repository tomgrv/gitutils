{
    "scripts": {
        "update": "npx npm-check-updates -i -u",
        "update-all": "npm run update -ws --root",
        "publish-beta": "bash -c 'git flow release start beta && npm run release --skip.tag'",
        "publish-prod": "bash -c 'git flow release finish beta --tagname $(git bump-version) --push'"
    },
    "commit-and-tag-version": {
        "scripts": {
            "prebump": "git bump-version"
        }
    }
}
