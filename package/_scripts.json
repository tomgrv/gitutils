{
    "scripts": {
        "update": "npx npm-check-updates -i -u",
        "update-all": "npm run update -ws --root",
        "publish-beta": "git release-beta",
        "publish-prod": "git release-prod"
    },
    "commit-and-tag-version": {
        "scripts": {
            "prebump": "git bump-version"
        }
    }
}
